name: Stock Price Forecast

on:
  schedule:
    - cron: '0 06 * * *'  # Schedule the job to run once a day at 06 AM (UTC)
  workflow_dispatch:  # Allows manual triggering

jobs:
  run_stock_price_forecasts:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip 
        pip install requests oauth2client scikit-learn pandas-gbq pandas numpy logging yfinance prophet statsmodels seaborn pygsheets google-auth-oauthlib google-auth-httplib2

    - name: Run the script
      run: python pipelilne_stockprice_forecast.py
